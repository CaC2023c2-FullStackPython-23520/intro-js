// Definición de funciones

function siguiente (n) {
    return n + 1
}

function doble (n) {
    return n * 2;
}

// Programa principal
let resultado = doble(siguiente(4));
console.log(resultado);


